# Deepfake Detection - Mercoledì 27 maggio 2026

## Deepfake detection methods

Possiamo fare deepfake detection usando sia approcci di machine learning che con approcci di deep learning, controllando artefatti spaziali e temporali. Riusciamo a discriminare contenuti reali e deepfake, l'architettura è in grado di capire quali sono gli elementi discriminativi per risolvere il task. Negli anni si è visto che questi approcci hanno limiti di robustezza, questi metodi soffrono del concetto di generalizzazione o maagri possono non funzionare con certi generatori.

## Fakespotter

L'idea è contare il numero di livelli che si attivano da una rete neurale, funziona ma è complicato fare questo conteggio, il conteggio sembra essere discriminativo con architetture mai viste prima.

## Detecting and simulating artifacts in GAN Fake Images

Vogliamo cercare di trovare un approccio in grado di estrarre una traccia indipendentemente dalla semantica. Deve essere invariante alla semantica ma la traccia che estraggo deve essere fortemente discriminativo, il problema è che la traccia magari essite ma non siamo ancora in grado di estrarla potrebbe essere nel dominio frequenziale o di fourier, potrebbe essere visibile o potrebbe non esserlo. Lo spazio latente potrebbe essere più discriminativo dello spettro di Fourier, potremmo anche considerare poiù approcci, è un problema aperto quello della generalizzazione e si cerca ogni giorno di affrontarlo al meglio.

## DFDT

Abbiamo un boom dei vision transformers, abbiamo performance molto elevate, le architetture sono ribuste, nella deepfake detection dobbiamo lavorare su diversi domini, è più robusto ma abbiamo dei limiti. Questo approccio è basato solo sulle immagini non funzionerebbe nel dominio dei video

## State of Art

Nel 2014 si inizia a parlare di generalizzazione, molti sono i metoddi emergenti

## LAA-NET

Vogliamo simulare il possibile artefatto creando degli pseudo fake creando delle maschere per capire dove è presente l'artefatto, questo mi permette di discriminare bene tra dati reali e dati sintetici. 

## AVFF

Abbiamo anche approcci multimodali analizzando flussi diversi di input, oltre al flusso di fotogrammi abbiamo anche lo stream audio

## CVPR 2024

Anche nei video si tende a generalizzare, si possono creare contenuto totalmente sintetici, l'artefatto potrebbe essere presente in qualsiasi altra regione del fotogramma

## Beyond Deepfake Images

Nel dominio frequenziale ci sono anomalie, ci sonno regioni che mi caratterizzano le immagini. Oggi si tende a creare degli agenti che cercano scenari dove abbiamo troppa alta frequenza

## Exploiting the signal leak

Cerchiamo tracce anche in altri domini ogni volta chre aggiugniamo rumore nascondiamo la semantica ma mettiao in risalto elementi discriminativi del modello, questo aapproccio funziona abbastanza bene, se noi avessimo queste informazioni possiamo avere tracce del modello generativo, vogliamo cercare queste informazioni nello spazio latente, non guardiamo più lo spazio RGB o Fourier.

## La maggiorparte dei detector esistenti

Prendo una baseline e do in input dei dati, i foundation models funzionano bene rispetto al passato

## DeepFeatureX Net

Vogliamo mettere in risalto feature discriminative, sfruttiamo il problema dell'overfitting per discriminare se un contenuto è reale o manipolato, abbiamo 3 base model creati dando tantissimi esempi di una tipologia e poche delle altre, quindi avremo un modello che fa overfitting sulle GAN, una sui Diffusion model, una sulle imamgini reali. Riuscirà a discriminare bene un contenuto reale da uno manipolato, le backbone vengono freezate, si concatena e si da in input ad una CNN in grado di svolgere il task di classificazione.

## Robustness Real GAN vs DM

Considerando attacchi di varia natura, in base alla compressione ed al QF ci sono architetture più robuste rispetto a compressioni più pesanti, con QF50 che è la compressione più usata dai social netwoek è la più interessante, 

## Comparison with SOTA

Un confronto generico con varie architetture all'avanguardia, l'approccio proposta supera nella maggior parte dei casi gli approcci allo stato del'arte, prendo un dataset lo testo su tanti altri a variare di queste combinazioni di training vediamo quali sono le performance del discriminatore, questo approccio è basato su un concetto di overfitting e da risultati migliori

## Basic Models

Abbiamo un problema, la robustezza. Come la risolviamo? Con la data augmentantion, non sempre porta a dei risultati sicuri, sfruttiamo un approccio badato su reti siamesi. Tramite la contrastive loss si cerca di raggruppare le rpapresentazioni e creare un cluster unico raggruppando il più possibile, dando 2 input si utilizza un architettura con pesi condivisi e comparando tramite contrastive loss, minimizziamo la loss se appartengono alla stessa classe e massimizziamo se appartengono a classi diverse. Una volta che abbiamo allenato prendo l'architettura per risolvere il task di classificazione e aggiungo uno o più layeer FC per fare classificazione ad L classi. Inizialmente abbiamo solo 2 classi, durante il traininng i pesi che abbiamo tarato non li tocchiamo, alleniamo solo gli ultimi layer per capuire a quale classe deve appartenere.

## DeepFeature X-SN

Vogliamo avere un descrittore più robusto usando le reti siamesi, il resto è uguale a prima facendo data augmentation riesxo ad ottenere performance migliori

## T-SNE Representation

In medio si ottengano performance migliori rispetto a tutti i modelli visti prima, la cosa interessante è che se prendessimo questi ddescrittori e provassimo a plottarli in uno spazio 2D e 3D, vediamo una distinzione tra le categorie di modelli generativi, vogliamo fare deepfake attribution, della tecnologia utilizzata, andiamo a definire la tecnologia tra diffusion model e GAN, è un primo passo verso l'attribution.

## Conclusioni

Ci spostiamo su qqualcosa che faccia anomaly detection, passiamo ad un modello una grande quantità di immagini real ogni volta che riceve una immagine fake vede che sono presenti anomalie rispetto al dataset di partenza, questo approccio generalizza bene.

# Deepfake Attribution & Deepfame Model Recognition

Il modello è un insieme di pesi, univoco per ogni istanza di una data architettura, se nooi riusciamo a discriminare rispetto a questa differenza minima il task non è complicato, se noi prendiamo un architettura inevitabilmente performiamo bene, significa che il task non è cosi complicato.

## A generic pipeline

Cosa dovrebbe avere a disposizone un esperto forense? In primo livello vogliamo capire se il contenuto è reale o manipolato quindi il classico task di deepfake detection, vogliamo capire quale tecnologia è stata utilizzata per la creazione

## Definizione

Dati degli input vogliamo capire quale modello ha causato la manipolazione di quel dato, per affrontare task del genere risulta essere necessario avere a disposizone il dataset.

## WILD

Dataset piccolo, si allena un architettura general purpose e ssi cerca se in un contesto ristretto riusciamo a discriminare, performa abbastanza bene ma in un contesto open iniziano i problemi vogliamo generalizzare su architetture mai viste. Generalizzare l'attribution è un concetto ancora da definire, non è come la deepfake detection o è real o è fake, qui entraimo nel dettaglio come facciamp ad attribuire qualcosa mai vista durante il training. In qualche modo questo paradigma potrebbe aiutarci. L'altro approccio è utilizzare una sorta di soglia, se la probabilità è bassa dirò che è stata generata da qualcos altro faccio smash dell'input è un approccio che se uso dati sintetici in generale il dubbio è ma l'input era real o generato da qualche architettura. 

## Le sfide della deepfake attribution

Inizia a diventare un task più complicatp della classica classificazione binaria è un task più complicato della deepfake detection, i dati sisntetici si evolvono, i modelli di stable diffusion sono tantissimi, tutto è sempre più complicato, un approccio di metric learning potrebbe andare. Un problema è la sovrapposizione di strumenti, prendo un immagine real, l'immagine viene manipolata da un tool che mi aggiunge gli occhiali, l'immagine la modifico nuovamente con un altro tool, posso dire qual è l'architettura che ha manipolato l'immagine? Posso dire quali sono tutti i tool utilizzati? Deepfake attribution in profondità. In realtà l'attaccante potrebbe prendere tutta la parte di codice, prende i tool esistenti open e riesce a risalire quale tool specifico è stato utilizzato durante la manipolazione, faccio una modifica affinchè il mio approccio generativo lasci una traccia differente, molto simile all'originale ma diversa.

## Come funziona l'analisi forense?

Facciamo analisi tramite reti neurali, tramite metodi contrastive

## Closed Set vs Open Set

Si studia in contesti chiusi, spesso in open set abbiamo problemi, possiamo usare approcci di metric learning o approcci con treshold, questo non significa che stiamo facendo deepfake detection, la deepfake attribution non è deepfake detection è un task molto più complicato.

## Metric Learning

Possiamo pensare di usare le triplet, simile alle reti siamesi, abbiamo ancora, positivo e negativo il positivo appartiene alla stessa classe dell'ancora, quello negativo appartiene ad un'altra classe dell'ancora. Tendiamo ad allontararli per cercare di ottenere una suddivisone omogenea.

## Hierarchical Attribution

Nella parte di attribution l'architettura mi dice che l'immagine è fake, semplifichiamo il problema non vogliamo analizzare tutti i pattern il problema è più semplice, passo quindi a definire qual è la sorgente specifica rispetto alla tecnologia

## Ensemble System

Possiamo sfruttare questi apporcci per ottneere metodi sofisticati, se riusciamo a dire qual è l'architettura siamo già un passo avanti.

## Future Research Directions

Gli attacchi avversariali devono funzionare in contesti puramente realistici, gli approcci di attribution nascono ogni anno dove si cercava di capire qual è rispetto alle mie tre classi si cerca di capire se ci sono texture discriminative tra due tracce, vogliamo discriminare rispetto a tre architetture.

## Dataset

Approccio che prevede la suddivione del dataset

## Domini di Fourier

Vediamo se è presente una traccia, si vede che il concetto è gerarchico, vogliamo discriminare real vs AI, arriviamo a livello 3 solo se siamo passati al livello 2 e viceversa, in base al task che stiamo affrontando ripercorriamo al conrtrario l'albero rispetto al classificatore, come vediamo nel livello 1 e livello 2 è classificazione binaria riusciamo a discriminare abbastanza bene. Facciamo un analisi più dettagliata per capire la natura del dato sintetico

## Generalizzazione

L'approccio continuana funzionare anche in altri livelli usiamo tecnologie diverse in differenti task di face forensics

## Deepfame Model Recognition Task

Facciamo fine tuning, generiamo immagini ed iterando collezionaiamo tanti modelli, n modelli questo possiao farlo con stylegan2 otteniamo 100 diversi modelli, ci chiediamo qual è il modello specifico utilizzato lo prendiamo dalla rete ufficiale di stylegan per stimare i pesi del modello.

## StyleGAN2-ADA

Si basa su StyleGAN2 fa Data Augmentation con meno dati rispetto a styleGAN2 

## The Models

Considerato le varie coppie k e p raggiungo 100 diverse istanze di stylegan2 ada. Dal punto di vista architetturale l'architettura è quella i pesi del generatore e del discriminatore risulteranno essere quasi identici, tutti i pesi sono freezati allpeccezione dell'ultimo layer convolizionale. Anche dal punto di vista visivo non abbiamo nette differenze.

Dal punto di vista di creazione quando applichoiamo convoluzione verrà lasciata una traccia diversa da ogni immaigine, pian piano si inizia a vedere qualcosa man on è immediato notare queste differenze

## Training and test data defintion

Faccio un approccio preliminare se alleno una resnet18 classifico solo i primi 50 modelli, riusciamo  a discriminare bene rispetto alle 50 classi, in qualche modo con una resnet18 abbiamo risolto un task preliminare, vogliamo generalizzare, usiamo il metric learning, lo usiamo rispetto agli embedding finali ma la rappresentazione finale sono dei descrittori orientati a risolvere al meglio il mio task rispetto alle mie 50 classi, non a generalizzare nel nostro contesto i primi layer sono i più discriminativi più andiamo a prendere dettagl icaratteristici per riolvere il nostro task, conviene che prendo in considerazione le informazioni che posso estrarre dai primi layer, pertanto il metric learing non lo applichiamo all'ultimo layer ma ai descrittori che posso estrarre nell'ultimo layer. Si possono usare tecniche più avanzate come la SVG molto più performante nel rilasciare dati discriminativi vengono comparati i descrittori.

## Deepfake Model Recogntion Task

Calcoliamo la distanza euclidea vediamo se questa distanza appartiene allo stesso modello, se sono vicine apparterranno allo stesso modello altrimenti a modelli differenti. 


