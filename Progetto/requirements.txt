# Core deep learning (su Colab torch/torchvision sono già presenti:
# installarli solo in locale, su Colab si possono commentare).
torch>=2.1
torchvision>=0.16

# Numerico / immagini
numpy>=1.24
pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
pyyaml>=6.0

# VLM agent (explainability) — Qwen2.5-VL
transformers>=4.49
accelerate>=0.34
bitsandbytes>=0.43        # quantizzazione 4-bit (richiede GPU CUDA, es. T4)
qwen-vl-utils>=0.0.8      # helper opzionale per Qwen2.5-VL
sentencepiece>=0.1.99
